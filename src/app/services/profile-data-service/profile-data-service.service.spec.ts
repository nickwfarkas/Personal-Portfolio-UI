import { ProfileDataService } from './profile-data-service.service';

describe('ProfileDataService', () => {
  it('should create an instance', () => {
    expect(new ProfileDataService()).toBeTruthy();
  });
});
