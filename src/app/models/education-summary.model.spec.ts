import { EducationSummary } from './education-summary.model';

describe('EducationSummary', () => {
  it('should create an instance', () => {
    expect(new EducationSummary()).toBeTruthy();
  });
});
